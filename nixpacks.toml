[variables]
NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm@latest",
  "npx pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["npx pnpm run build"]

[start]
cmd = "node server.js"

